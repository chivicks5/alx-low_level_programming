#include "main.h"
/**
 * print alphabet - print lower case alphabet only
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	_putchar(letter);
	-putchar('\n');
}
